import { PlaceholderScreen } from "./PlaceholderScreen";

export function ObsDock() {
  return (
    <PlaceholderScreen
      title="OBS dock"
      subtitle="The narrow panel that lives inside OBS during a stream. Comes online with detection."
    />
  );
}
