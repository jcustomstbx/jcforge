import { PlaceholderScreen } from "./PlaceholderScreen";

export function Detection() {
  return (
    <PlaceholderScreen
      title="Detection"
      subtitle="Tune the weights that decide what counts as a moment. Built once the signals exist."
    />
  );
}
