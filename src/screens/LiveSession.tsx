import { PlaceholderScreen } from "./PlaceholderScreen";

export function LiveSession() {
  return (
    <PlaceholderScreen
      title="Live session"
      subtitle="The during-stream view. Ingest preview, excitement timeline and caught-clips feed land in a later milestone."
    />
  );
}
