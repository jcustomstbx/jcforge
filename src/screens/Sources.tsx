import { PlaceholderScreen } from "./PlaceholderScreen";

export function Sources() {
  return (
    <PlaceholderScreen
      title="Sources"
      subtitle="Connection state and where clips go. OBS and Twitch source cards land in a later milestone — connection state is already live in the title bar."
    />
  );
}
