import { PlaceholderScreen } from "./PlaceholderScreen";

export function ClipLibrary() {
  return (
    <PlaceholderScreen
      title="Clip library"
      subtitle="Review everything caught. Populates once clips start landing on disk."
    />
  );
}
