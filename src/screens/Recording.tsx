import { PlaceholderScreen } from "./PlaceholderScreen";

export function Recording() {
  return (
    <PlaceholderScreen
      title="Recording"
      subtitle="Quality settings JCForge writes into OBS's replay buffer. Built alongside the OBS settings sync."
    />
  );
}
