# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Rust / Tauri
src-tauri/target

# Sidecar binaries and models - too large for git. Fetched/built by
# scripts/fetch-ffmpeg.ps1 and scripts/build-whisper.ps1 instead; see
# src-tauri/binaries/NOTICE.md.
src-tauri/binaries/*.exe
src-tauri/models/*.bin

# Redundant copy of design_handoff_jcforge, kept locally only
/AI-powered stream clip creator.zip

# Packaged test builds sent to testers - large, rebuildable, not source
/JCForge_Test_Build.zip

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
